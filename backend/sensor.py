def process_temperature(room, temperature):
    if(temperature > 30)
    
def process_luminance(room, luminance):

def process_humidity(room, humidity):
    if(humidity > 60):
        close_blinds(room)


def close_radiator(room, value=0):
    # Contact KNX to close blind

def open_radiator(room, value=255):
    # Contact KNX to open blind

def close_blinds(room, value=0):
    # Contact KNX to close blind

def open_blinds(room, value=255):
    # Contact KNX to open blind

def process_sensor(data):

